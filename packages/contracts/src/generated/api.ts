/**
 * PLACEHOLDER — overwritten by `npm run contracts:generate`.
 *
 * Until the generator runs against a live backend, these are empty shells so
 * that `import type { paths } from '@bizvision/contracts'` type-checks.
 */

export type paths = Record<string, never>;
export type components = Record<string, never>;
export type operations = Record<string, never>;
