# ============================================================
# BizVision AI — Backend DEV/CI dependencies
# Lightweight tooling for lint, types, and tests. The heavy
# runtime/ML stack lives in requirements.txt.
# ============================================================

# ─── Lint / Format / Types ─────────────────────────────────
ruff==0.15.15
mypy==1.10.1
types-redis==4.6.0.20240425
types-python-jose==3.5.0.20260408

# ─── Testing ───────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.28.1          # FastAPI TestClient / async client
asgi-lifespan==2.1.0   # exercise startup/shutdown in tests
aiosqlite==0.22.1      # in-memory async DB for fast unit tests

# ─── Dev ergonomics ────────────────────────────────────────
pre-commit==3.7.1
